/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  loader: 'custom',
}

module.exports = {
  ...nextConfig,
  // images: {
  //   domains: ['gateway.ipfscdn.io'],
  // },
}
